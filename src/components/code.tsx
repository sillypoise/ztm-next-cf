function Code() {
    return (
        <>
            <p> Below is some code, you can copy it with <kbd>Ctrl-C</kbd>. Did you know, <code>alert(1)</code> can show an alert in JavaScript! </p>
            <pre><code>{`// This logs a message to the console and check out the scrollbar.<br>console.log('Hello, world!')
function test() { consol.log("hello world")}
`}</code></pre>
        </>
    )
}

export { Code }
